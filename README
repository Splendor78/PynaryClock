===PynaryClock===

A simple clock that continuously updates and displays the time on the 
command line/terminal in both binary and digital read-out.

To run the clock simply run the pynaryclock.py file in your preferred console/
terminal.